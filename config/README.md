# Google Sheets service-account setup

Place the downloaded Google Cloud service-account key in this directory as:

`google-service-account.json`

The key file is ignored by Git and this directory is blocked from direct Apache access. Never commit or email the private key.

